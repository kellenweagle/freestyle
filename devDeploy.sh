#!/usr/bin/env bash

# TODO(kellen): validate database is running
# TODO(kellen): add logging

# LEFTOFF(kellen): created the route for create product, need to add to the browser and do the redux and react stuff for create product.

# This runs commands from the root package.json and it starts the frontend and backend in the same terminal :)
npm run start
npm run run